import React from 'react'

const useGetBookingCat = () => {

    // use set to get:

    // status

    // type

    // brand (only brand needs to hardcode the brand names COLAB & ITCD)
    return (
        <div>useGetBookingCat</div>
    )
}

export default useGetBookingCat